# A default java.util.logging configuration.
# (All App Engine logging is through java.util.logging by default).
#
# To use this configuration, copy it into your application's WEB-INF
# folder and add the following to your appengine-web.xml:
# 
# <system-properties>
#   <property name="java.util.logging.config.file" value="WEB-INF/logging.properties"/>
# </system-properties>
#

# Set the default logging level for all loggers
.level=INFO

java.util.logging.ConsoleHandler.level=FINEST
java.util.logging.ConsoleHandler.formatter=com.google.util.logging.SimpleFormatter

# Specify logging levels for specific namespaces
com.google.apphosting.utils.jetty.level=WARNING
org.apache.commons.httpclient.level=WARNING
com.google.acre.level=FINEST
